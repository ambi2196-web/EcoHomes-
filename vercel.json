{
  "buildCommand": "pnpm install --no-frozen-lockfile && pnpm --filter desktop exec vite build",
  "outputDirectory": "apps/desktop/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
